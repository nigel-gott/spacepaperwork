strictness: high
test-warnings: true

ignore-paths:
  - .ipython

ignore-patterns:
  - (^|/)skip(this)?(/|$)

pyflakes:
  run: false

pylint:
  run: false

pep8:
  disable:
    - E501
  enable:
    - W601
  options:
    max-line-length: 88

mccabe:
  disable:
  - MC0001
